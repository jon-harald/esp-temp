# Copy to keystore.properties (git-ignored) and fill in for release signing.
# Debug builds use the auto-generated ~/.android/debug.keystore and ignore this.
storeFile=/absolute/path/to/release.jks
storePassword=REPLACE_ME
keyAlias=REPLACE_ME
keyPassword=REPLACE_ME
